{
  "$schema": "https://schema.railway.app/railpack.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install"
  },
  "deploy": {
    "startCommand": "cd backend && ${START_COMMAND:-npm start}",
    "restartPolicyType": "ON_FAILURE"
  }
}
